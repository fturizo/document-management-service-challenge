info:
  name: Retrieve Document URL
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/document-management/download/{{id}}
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

runtime:
  variables:
    - name: id
      value: "6"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
